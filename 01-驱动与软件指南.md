# 01 驱动与软件指南（HP EliteDesk 800 G4 Mini）

> 更新：2026-09-17 · 本文件回答"驱动在哪 / 软件装什么 / 镜像从哪下 / BIOS 怎么设 / 功耗方案"

## 一、驱动清单（全部在 ESP，无需额外安装）

**所有 17 个驱动（kext）都在 ESP 的 `EFI/OC/Kexts/`**，config.plist 全启用，开机由 OpenCore 自动加载。

| kext | 用途 |
|---|---|
| Lilu / VirtualSMC | 基础库 |
| WhateverGreen | 核显 UHD630 |
| AppleALC | 声卡 CX20632（layout-id 0x14） |
| IntelMausiEthernet | 有线 I219-V |
| itlwm | Wi-Fi AX200 **内核驱动**（连接管理靠 HeliPort app） |
| IntelBluetoothFirmware / IntelBTPatcher / BlueToolFixup | 蓝牙 AX200（**当前不工作，保留待硬件兜底**） |
| CPUFriend / CPUFriendDataProvider | CPU 功耗方案（勿改参数） |
| NVMeFix / RestrictEvents / SMCProcessor / SMCSuperIO / HibernationFixup / USBPorts | 存储优化 / 显示/功能 / 温度监控 / 休眠 / USB 定制 |

**加上**：OpenCore 引导器（BOOTx64 + OpenCore.efi）、6 个 UEFI Drivers、12 个 ACPI/SSDT、OpenCanopy 主题——全在 ESP，**自包含**。只拷 ESP 就能完整引导。

**唯一需要装到 macOS 系统里的软件 = HeliPort**（见下）。

## 二、HeliPort（Wi-Fi 连接软件）——下载 + 安装 + 用法

> itlwm 只负责驱动网卡，**扫描/输密码/连接/断连都靠 HeliPort**。装机后必装。

### 下载地址

| 来源 | 地址 | 说明 |
|---|---|---|
| **官方稳定版 v1.5.0**（推荐） | `https://github.com/OpenIntelWireless/HeliPort/releases/download/v1.5.0/HeliPort.dmg` | OpenIntelWireless 官方最新稳定版 |
| 官方开发版 v2.0.0-alpha | `https://github.com/OpenIntelWireless/HeliPort/releases/download/v2.0.0-alpha/HeliPort.dmg` | 测试版，一般不用 |
| 已装版本 | 本机现装 2.5.1（老吴恢复版自带），工作正常可继续用 | 版本不影响使用 |

### 安装

1. 下载 `HeliPort.dmg` → 双击打开 → 把 **HeliPort.app 拖入"应用程序"**
2. 打开 HeliPort → 菜单栏出现 Wi-Fi 图标
3. 系统设置 → 通用 → 登录项 → **添加 HeliPort**（开机自启）

### 用法

- 点菜单栏 HeliPort 图标 → 显示附近 Wi-Fi 列表 → 选网络 → 输密码 → 连接
- Option + 点图标：断开/更多选项
- 首次打开若提示"itlwm not loaded"：确认 ESP 里 itlwm.kext 已启用并重启

## 三、macOS Sequoia 15.6 镜像下载（当前系统来源）

> 当前系统 = **Sequoia 15.6（Build 24G84）老吴恢复版**，装机用 R-Drive Image 恢复（见 02 文档装机流程）。

### 下载地址（按优先级）

| 来源 | 地址 | 说明 |
|---|---|---|
| **老吴黑苹果工作室（本机来源）** | `https://hpglw.com/` | 首页有"macOS 各版本恢复版镜像下载"；Sequoia 15 恢复版约 14GB，天翼云盘分发；**本机就是用它装的** |
| **官方完整安装器**（InstallAssistant.pkg） | `https://swcdn.apple.com/content/downloads/05/14/082-08661-A_R3LJXRMIED/4ifxu7v20f45p4qectnt7r52qwt9vuzfw1/InstallAssistant.pkg` | Apple 官方 CDN 直链（15.6 build 24G84）；装完生成"安装 macOS Sequoia.app"；**国内直连较快** |
| 黑苹果屋 imacos（备选恢复版） | `https://imacos.top/2025/08/01/macos-sequoia-15-624g84/` | 官方纯净恢复版 15.6(24G84)，解压密码 `imacos.top` |
| IPSW（Apple Silicon 恢复固件） | `UniversalMac_15.6_24G84_Restore.ipsw`（osxdaily 有链接） | 本机 Intel 用不到，仅记录 |

> 提示：装机镜像记录已按"只保留当前 15.6"清理，15.7.x/15.7.9 等历史镜像测试记录不再保留。

## 四、BIOS 设置摘要（F10，详见装机手册）

| 关键项 | 值 |
|---|---|
| Secure Boot | **Legacy Support Disable + Secure Boot Disable** |
| BIOS Sure Start | **全部取消勾选**（否则回滚设置） |
| Intel ME | **勾选**（UHD630 休眠唤醒必需） |
| Wake On LAN | **Disabled**（休眠必需） |
| M.2 WLAN/BT + M.2 USB/Bluetooth | **勾选**（AX200） |
| Video Memory Size | 512MB |
| TPM | Hidden · No prompts |

## 五、CPU 功耗方案（已闭环，勿动）

- 目标：PL1=65W / PL2=85W（用户锁定口径）
- 实现：CPUFriend + CPUFriendDataProvider（LFM=0x08 800MHz / EPP=0x40 / PerfBias=0x04）
- 实测：全核 74-75W / 4.16-4.19GHz；空闲 **3.7-4.7W**（对齐 Win11 低负载 ~6W）；单线程 4.26-4.30GHz
- 教训（勿重演）：① BundlePath 带前缀 → Halting on critical error；② InsanelyMac 短字节补丁 → EXITBS 内核崩溃（均已修复/移除）

## 六、常见问题

| 问题 | 处理 |
|---|---|
| Wi-Fi 不工作 | ① 确认 itlwm.kext 在 ESP 且启用 ② HeliPort 已装 ③ 重启 |
| 为什么 Wi-Fi 显示"以太网" | itlwm 用 IOEthernetController，系统当以太网显示——**正常，不影响性能** |
| 耳机有声音麦克风没有 | 前面板上口是 TRRS 耳麦口，**必须用四段式耳麦**；下口纯耳机 |
| 蓝牙能修吗 | 已放弃（软件层穷尽），需要就买 USB 蓝牙免驱棒 |
| AirDrop 能用吗 | 不能（itlwm 无 AWDL），需换 BCM94360CS2 白果卡 |
