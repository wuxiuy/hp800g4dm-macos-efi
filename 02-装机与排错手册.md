# 02 装机与排错手册（HP EliteDesk 800 G4 Mini）

> 更新：2026-09-17 · 合并原装机/排错/约束/BIOS/历史记录，装机或改配置时对照本文件

---

## 一、操作铁律（改任何配置前必读）

1. **有据可依**：任何 config/EFI 修改先查权威来源（① OpenCore 官方 Configuration.pdf / Dortania ② 驱动官方 GitHub ③ InsanelyMac deeveedee 800 G4 Mini 帖 ④ ≥2 个独立来源交叉核实）。**禁止凭记忆写魔法数字**（Target/布局 ID/位掩码），写后注明出处。
2. **一次解决**：先完整设计方案再动手，不试错、不逐步试探；改完必须 `ocvalidate` 零错误 + 三方哈希一致 + 用户实机确认。
3. **防错盘**：磁盘0（512G）= Win11 **绝不碰**；磁盘1（256G MZVL2256HCHQ）= macOS；磁盘2 = U 盘（HFS+ 镜像勿动）。
4. **路径写法（官方语义）**：ACPI>Add 相对 OC/ACPI、Kernel>Add 相对 OC/Kexts、UEFI>Drivers 相对 OC/Drivers、Misc>Tools 相对 OC/Tools——**均无前缀**。曾因加 `Kexts\` 等前缀导致全部 kext 加载失败（卡苹果标 `Halting on critical error`）。
5. **BOOTx64.efi 必须保持原版 stub**（SHA1 `dd305e92c22e`，24KB）：作用是把引导重定向到 `EFI\OC\OpenCore.efi`。**严禁替换为 OpenCore 副本**（会报 `OC: Failed to load configuration!`，曾致双盘无法引导）。
6. **日志位掩码（OpenCore 1.0.x）**：写 .txt 日志用 `Target=67`（0x43）+ `DisplayLevel=2147483714`；调试完恢复 `Target=0`。0.9.x 定义不同不可混用。
7. **EFI 备份不要放进 ESP**（200MB 易爆），走 NAS / 本地目录 / git 双仓库。

---

## 二、装机流程（15.6 恢复版 / 原版安装）

### 方式 A：老吴恢复版（本机实际采用，最快）

1. 从 `https://hpglw.com/` 下载 **macOS Sequoia 15.6 恢复版**（~14GB，天翼云盘）
2. Windows 下用 **R-Drive Image 7**（`RDriveImage7.exe`）打开 `.rdr` 镜像 → 目标 = **磁盘1 分区2（APFS 容器 255.9GB）** → 恢复
3. 恢复完从磁盘1 引导 → OpenCore → macOS

### 方式 B：原版完整安装器（U 盘）

1. 下载官方 InstallAssistant.pkg（地址见 01 文档）→ 生成"安装 macOS Sequoia.app" → `createinstallmedia` 制作 U 盘（或 Etcher 烧 SharedSupport.dmg）
2. U 盘分区1 ESP 放本 EFI（564 文件）· 分区2 HFS+ 完整安装器
3. F9 → U 盘 → OpenCore → 安装器 → 磁盘工具抹磁盘1（APFS）→ 安装（**全程拔网线**）

### 装机注意事项

- U 盘插**后面板 USB 口**（直插主板最稳；"安装器已损坏"先换口再查 BIOS 时间）
- 防错盘：全程只认 238G（MZVL2256HCHQ），512G 是 Win11 立即取消
- 装后：Wi-Fi 需装 HeliPort（01 文档）；蓝牙不可用属已知
- 装后收尾：稳定 3~5 天 → 移除 U 盘（磁盘1 ESP 已有 EFI 可独立引导）；F9 双系统切换

---

## 三、OpenCore 使用

| 操作 | 说明 |
|---|---|
| F9 | 开机启动菜单 |
| F10 | BIOS 设置 |
| OC 方向键 + 回车 | 选系统 |
| Ctrl+Enter | 设为默认启动项 |
| Reset NVRAM | OC 菜单项（启动异常先试它） |
| 空格 | OC 菜单更多选项 |

---

## 四、排错表

| 现象 | 处理 |
|---|---|
| **卡苹果标 / Halting on critical error** | config 路径前缀错误（见铁律4）→ 按官方语义修正路径 |
| **引导失败 `OC: Failed to load configuration!`** | BOOTx64.efi 被换成 OpenCore 副本（见铁律5）→ 恢复原版 stub |
| **still waiting for root device（禁止符号）** | USB 口未被 USBPorts 覆盖 → 换后面板 USB 口；安装阶段可禁用 USBPorts（`config.plist.bak_usbports` 备份恢复） |
| **卡"剩余约15分钟"** | 非镜像/引导问题（两镜像同卡点，硬盘灯不亮）→ 换 USB 口 / 查 BIOS 时间 |
| **安装器要求联网** | 拔网线（本地数据安装，不该联网） |
| 引导黑屏/卡住 | 开日志（Target=67）→ 读 `opencore-*.txt` |
| OC 菜单不出现 | SecureBoot 未关 / USB Boot 未开 / ESP 文件缺失 |
| 休眠唤醒黑屏 | BIOS ME 开启 / WakeOnLAN 禁用 |
| 声卡无声 | AppleALC layout-id 0x14；VoodooHDA 未启用 |
| 睡眠后立即唤醒 | 备选：SSDT-GPRW + GPRW→XPRW 补丁（会失去键鼠唤醒，遇到再用） |
| 蓝牙断连/开关卡死 | 已整体放弃（软件层穷尽，见 03 文档） |

---

## 五、已修复历史问题（勿重蹈覆辙）

| 日期 | 问题 | 根因 | 修复 |
|---|---|---|---|
| 2026-09-15 | 卡苹果标 | config 路径双前缀 `Kexts\Kexts\...`（commit 5f3d857） | 按官方语义修正 31 项，ocvalidate 通过 |
| 2026-09-15 | 双盘无法引导 | BOOTx64.efi 换成 OpenCore 副本 | 恢复原版 stub（dd305e92） |
| 2026-09-15 | still waiting for root device | USB 口未被 USBPorts 覆盖 | 换口 + 安装期禁用 USBPorts |
| 2026-09-16 | CPUFriend Halting | BundlePath 误带 `OC/Kexts/` 前缀 | 修正路径 |
| 2026-09-17 | CPUFriend EXITBS 崩溃 | InsanelyMac 短字节补丁 `74 0F→EB 0F` Count=0 | 移除补丁 |
| 2026-09-17 | 蓝牙 Block 实验 | 屏蔽 IOBluetoothHostControllerUSBTransport 无效 | 回滚 Block，恢复稳定版 |

---

## 六、时间修正（RealTimeIsUniversal）

- Windows 注册表：`HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\TimeZoneInformation` 建 `RealTimeIsUniversal` DWORD=1（让 Win 与 macOS 都用 UTC 硬件时钟，避免双系统时间差 8 小时）
- macOS 侧默认即 UTC 硬件时钟，无需改动
