# 03 升级与蓝牙记录（HP EliteDesk 800 G4 Mini）

> 更新：2026-09-17 · 合并原 Tahoe26 方案 + Wi-Fi/蓝牙修复记录 + 09-17 终局结论的专项部分

---

## 一、系统升级规划

### 1. Sequoia 15.7.8（可直接升，低风险）

- 同内核代维护版；**itlwm 2.3.0 + HeliPort 在 15.7.x 实证正常**（InsanelyMac 15.7.5 帖）
- kext 兼容不变（声卡/睡眠/功耗/有线均不受影响）
- 流程：备份（Time Machine/磁盘映像）→ 系统设置 → 软件更新 → 升 15.8 → 重启验证（Wi-Fi/声卡/睡眠/功耗/有线）
- 用户路线：**先升 15.8 → 再评估 Tahoe 26**

### 2. Tahoe 26（能上，需预置补丁）

- 8 代 CPU **不在官方支持列表** → 必须预置 **Booter Patch「Apple | Skip Board ID check」**
- 同机型实锤案例：HP EliteDesk 800 G4 + i7-8700 安装器黑屏 = Board ID check（非显卡）
- OpenCore 1.0.7 ≥ 要求 1.0.4 ✅
- 升级前需：备份 + 更新 Lilu/WhateverGreen/AppleALC + 老吴 Tahoe 26 恢复版测试
- **补丁对 Sequoia 无副作用**（Board ID 本就在列表，补丁 pass 或被跳过）

> 补丁完整 Find/Replace 值在旧版 `00-Tahoe26支持方案-20260916.md`（已并入历史提交），如需要可 `git log` 找回。

---

## 二、蓝牙修复全记录（终局：放弃）

### 结论

**AX200 蓝牙软件层彻底穷尽，用户拍板放弃**。兜底 = USB 蓝牙免驱棒（未购买）。

### 判定证据链

| 证据 | 内容 |
|---|---|
| ioreg 枚举 | `IOUSBHostDevice@14800000`：idVendor=0x8087 / idProduct=0x29 / PortNum=8 / Built-In=Yes（枚举正常） |
| 驱动实例 | `IntelBluetoothFirmware` **`!registered,!matched,fw_name=""`**——驱动加载但 HCI 握手失败 |
| 假身份 | SPBluetoothDataType 显示 `Chipset: BCM_4350C2 / Vendor: 0x004C (Apple)`——Apple 栈误认 |
| Win11 对照 | AX200 蓝牙完全正常（VID_8087&PID_0029，ibtusb）——**硬件没问题** |
| ACPI 对照 | 蓝牙口 = `\_SB.PCI0.XHC.RHUB.HS14`；USBPorts HS14 配置正确（type 255/255、2100/3200mA） |

### 已试全部方案（全部无效，勿重试）

1. 官方 2.4.0 / 2.3.0 / lshbluesky 2.5.1 RELEASE + DEBUG
2. IntelBTPatcher + BlueToolFixup 组合
3. boot-args `-btlfxbeta -btlfxallowanyaddr`
4. NVRAM 三蓝牙变量
5. SSDT-USBX
6. HS08 供电修正（2100/3200mA）
7. **Power Cycle**（关机→拔电→长按电源键 60 秒放电——社区顶级修复）——**失败**
8. **Kernel/Block 屏蔽 `com.apple.iokit.IOBluetoothHostControllerUSBTransport`**——屏蔽生效（transport 消失）但 IntelBluetoothFirmware 依旧 `!registered,!matched,fw_name=""` → **与 Apple 栈抢占无关，是 Intel 驱动自身 HCI 握手失败**

### 终局判定

- 软件层穷尽；Block 已回滚，ESP 恢复干净稳定版
- 硬件正常（Win11 可用）——是黑苹果软件栈对 AX200 蓝牙的兼容边界
- 附带结论：HS14 端口配置正确；HS08 误标蓝牙（原"Front Left USB2"）暂保留无危害

---

## 三、AirportItlwm 考证（结论：不切换）

### 结论

**维持 itlwm 2.3.0 + HeliPort**（用户确认"不搞了，就这样吧"）。

### 考证证据

| 考证项 | 结果 |
|---|---|
| 官方 OpenIntelWireless itlwm v2.3.0 | AirportItlwm 最高只到 **Sonoma 14.4**——**Sequoia 15.x 无官方稳定版** |
| lshbluesky/itlwm 分支 | **404 不存在** |
| Alpha Prerelease | GitHub Actions 自动编译，官方不推荐日常用 |
| 官方 FAQ 功能边界 | 仅 Handoff + 通用剪贴板、WPA2/3-Enterprise、定位服务；**AirDrop 依然不支持** |

### 本机实际收益评估（换 = 几乎零收益）

| 功能 | 声称支持 | 本机实际 |
|---|---|---|
| Handoff 接力 | ✅ | ❌ 依赖蓝牙（已死），用不了 |
| 通用剪贴板 | ✅ | ❌ 依赖蓝牙（已死），用不了 |
| WPA2/3-Enterprise | ✅ | ⚠️ 家用网络用不到 |
| 定位服务 | ✅ | ⚠️ 唯一可能有用，需 iCloud + 精度存疑 |

**结论**：Sequoia 无官方稳定版 + 换装冒 Wi-Fi 失效风险 + 功能几乎落空 → **不切换**。历史 `.bak`（AirportItlwm 曾被替换）也印证此路线不靠谱。

---

## 四、Wi-Fi / 蓝牙技术背景（为什么这样配）

- **Sequoia 15 移除 IO80211FamilyLegacy** → Intel 无线必须用降级栈：Kernel/Block 屏蔽系统 IOSkywalkFamily（Enabled）+ 加载 itlwm 2.3.0
- itlwm 用 Apple 公共 **IOEthernetController**（显示为"以太网"属正常设计）；**HeliPort** 是连接管理 app（itlwm 只管内核层）
- AirDrop / 接力 / 连续互通 = **AWDL 协议**，itlwm / AirportItlwm 均不支持 → 永久不可用，唯一解 = 换 BCM94360CS2 白果卡（硬件改造，不计划）
